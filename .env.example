# Database credentials for Liane's Library
# Copy this file to .env and fill in your own values.
# .env is git-ignored and must never be committed.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=lianes_library
